#include <stdio.h>

int main()
{
int columns;
int rows;
for (rows = 1; rows <= 10; rows++);
	{
	for (columns = 1; columns <= 1000; columns * 10);
		{
		printf("%d", columns);
		}
	printf("\n");
	}
return 0;
}
